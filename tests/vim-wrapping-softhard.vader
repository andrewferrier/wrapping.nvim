Execute (plugin is loaded):
  AssertEqual 1, g:loaded_wrapping_softhard

Execute (wrapmode autodetected to soft):
  edit longlinefile.txt
  AssertEqual 1, &wrap

Execute (wrapmode autodetected to hard):
  edit shortlinefile.txt
  AssertEqual 0, &wrap
